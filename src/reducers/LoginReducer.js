export default (state = null, action) => {
    switch(action.type) {
        case "LOGIN":
            var user = undefined;
            return user;
        default: 
            return state;
    }
}